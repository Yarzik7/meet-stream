import { Typography } from "@mui/material";

const Header = () => {
  return (
    <header>
      <Typography variant="h1" align="center">
        MeetStream
      </Typography>
    </header>
  );
};

export default Header;
